import React from "react";

/** Compact brand mark (collapsed nav / meta) — "E." in editorial serif. */
export const Icon: React.FC = () => (
  <span className="e-icon">
    E<span className="e-dot">.</span>
  </span>
);

export default Icon;
